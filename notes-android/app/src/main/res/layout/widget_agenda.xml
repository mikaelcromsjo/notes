<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/widget_background"
    android:padding="12dp">

    <TextView
        android:id="@+id/widget_summary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Loading…"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="@color/widget_text_primary" />

    <!-- Every overdue/today/to-do/orphan item, one per row, in a real
         scrolling list (AgendaWidgetService/AgendaRemoteViewsFactory) instead
         of a fixed handful of lines — a widget only has so much height, but
         nothing here should be truncated to fit it; it should scroll. -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="4dp">

        <ListView
            android:id="@+id/agenda_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@null"
            android:dividerHeight="0dp"
            android:scrollbars="none" />

        <TextView
            android:id="@+id/agenda_empty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp" />

    </FrameLayout>

</LinearLayout>
